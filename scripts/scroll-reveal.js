(function(){const revealItems=Array.from(document.querySelectorAll(".rv")),cardItems=Array.from(document.querySelectorAll(".card-enter")),dividerItems=Array.from(document.querySelectorAll(".dv"));if(!revealItems.length&&!cardItems.length&&!dividerItems.length)return;const revealNow=()=>{revealItems.forEach(el=>el.classList.add("vis")),cardItems.forEach(el=>el.classList.add("vis")),dividerItems.forEach(el=>el.classList.add("vis"))};if(prefersReducedMotion||typeof IntersectionObserver!="function"){revealNow();return}if(revealItems.length){const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.isIntersecting&&(entry.target.classList.add("vis"),revealObserver.unobserve(entry.target))})},{threshold:.08,rootMargin:"0px 0px -8% 0px"});revealItems.forEach(item=>revealObserver.observe(item))}if(cardItems.length){const cardObserver=new IntersectionObserver(entries=>{entries.filter(entry=>entry.isIntersecting).forEach((entry,index)=>{const delay=Math.min(index*55,180);window.setTimeout(()=>{entry.target.classList.add("vis"),cardObserver.unobserve(entry.target)},delay)})},{threshold:.05,rootMargin:"0px 0px -6% 0px"});cardItems.forEach(item=>cardObserver.observe(item))}if(dividerItems.length){const dividerObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.isIntersecting?entry.target.classList.add("vis"):entry.target.classList.remove("vis")})},{threshold:0});dividerItems.forEach(item=>dividerObserver.observe(item))}})();
