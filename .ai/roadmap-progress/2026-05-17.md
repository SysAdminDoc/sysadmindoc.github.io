# Roadmap Progress

Date: 2026-05-17

## Batch 1: Tier 0 Production Dependency Remediation

Completed roadmap item:

- `[x] 1. Fix production dependency advisories`

Changes:

- Upgraded `astro` from `^5.1.0` to `^6.3.3`.
- Upgraded `marked` from `^18.0.0` to `^18.0.3`.
- Upgraded `sanitize-html` from `^2.17.2` to `^2.17.4`.
- Upgraded `@astrojs/check` from `^0.9.8` to `^0.9.9`.
- Refreshed transitive `devalue` to 5.8.1 and `postcss` to 8.5.14 in `package-lock.json`.
- Added `npm run audit:prod`.
- Added the production audit gate to `.github/workflows/deploy.yml`.

Verification:

- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 24 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 181 pages built.

Notes:

- Full `npm audit` still reports dev-only moderate findings through `@astrojs/check` and its language-server dependency chain. The current production gate is intentionally scoped to high/critical production advisories.
- Next roadmap item: `[ ] 2. Reconcile the live GitHub catalog against src/data/projects.ts`.

## Batch 2: Catalog Drift Audit and Reconciliation

Completed roadmap item:

- `[x] 2. Reconcile the live GitHub catalog against src/data/projects.ts`

Changes:

- Added `OpenLumen`, `PhoneFork`, and `AI-Usage_Tracker` to `src/data/projects.ts`.
- Added `src/data/catalog-policy.json` with intentional public-repo exclusions and the `RadAtlas` privacy-review hold.
- Added `scripts/audit-catalog.mjs`, which parses portfolio project refs with the TypeScript compiler API and compares them against live GitHub public repos.
- Added `npm run catalog:audit`.
- Added the catalog audit to the GitHub Pages deploy workflow with `GITHUB_TOKEN`.
- Updated README counts and version surfaces to v0.16.2.
- Refreshed ignored generated GitHub caches with `GITHUB_TOKEN`: 170 public repo metadata entries, 220 stars, 60 releases, and 170 READMEs.

Verification:

- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run fetch-stars` with `GITHUB_TOKEN`: refreshed ignored generated data successfully.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 24 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.

Notes:

- The audit tracks active public non-forks by default. Public forks may remain cataloged intentionally, but they are not treated as missing owned-source drift.
- Reviewed public repos not cataloged: `ChanPrep`, `Scripts`, `SysAdminDoc`, `null`, and `RadAtlas`.
- Next roadmap item: `[ ] 3. Resolve the medical-imaging public boundary`.

## Batch 3: Medical-Imaging Boundary

Completed roadmap item:

- `[x] 3. Resolve the medical-imaging public boundary`

Changes:

- Kept `RadAtlas` excluded from `src/data/projects.ts`.
- Expanded `src/data/catalog-policy.json` with `GeneratorSpecs` as a removed medical-imaging artifact.
- Strengthened `scripts/audit-catalog.mjs` so privacy-review repos fail the audit if they appear in project data or public screenshots.
- Removed tracked stale screenshots `public/screenshots/RadAtlas.jpg` and `public/screenshots/GeneratorSpecs.jpg`.
- Documented that upstream GitHub visibility changes remain explicit owner actions outside this repo.

Verification:

- `npm run catalog:audit`: passed; no unreviewed drift and no privacy-review portfolio assets.

Notes:

- `RadAtlas` remains public on GitHub as of this pass, but the portfolio no longer promotes or stores visual artifacts for it.
- Next roadmap item: `[ ] 5. Move project data behind a schema-checked content layer`.

## Batch 4: Schema-Checked Project Data

Completed roadmap item:

- `[x] 5. Move project data behind a schema-checked content layer`

Changes:

- Added `scripts/validate-project-data.mjs`.
- Added `npm run data:validate`.
- Made `npm run check` and `npm run build` run data validation before Astro.
- Added a GitHub Actions validation step.
- Added `src/data/categories.ts` for shared category labels.
- Tightened `Featured.lang` to the `Lang` enum.
- Added missing `HurricaneMap` and `ApocalypseWatch` screenshots so all live apps have tracked thumbnails.
- Updated README, changelog, project context, and roadmap state to v0.16.3.

Verification:

- `npm run data:validate`: passed; 9 featured, 22 live apps, 173 catalog entries, 173 unique project routes, 22 screenshots checked.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 25 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.

Notes:

- The schema-check path intentionally keeps `src/data/projects.ts` as the editable source of truth instead of forcing an Astro content collection migration in this batch.
- Next roadmap item: `[ ] 6. Split generated data refresh from deployment`.

## Batch 5: Split Generated Data Refresh

Completed roadmap item:

- `[x] 6. Split generated data refresh from deployment`

Changes:

- Added `scripts/summarize-generated-data.mjs`.
- Added `npm run data:summary`.
- Added `.github/workflows/data-refresh.yml` for scheduled/manual metadata refresh without deployment.
- Removed the schedule trigger from the deploy workflow.
- Kept deploy builds fresh by requiring `GITHUB_TOKEN`, running `npm run fetch-stars`, summarizing generated data, and uploading `github-data-refresh-summary` before Astro checks/build.
- Ignored local data-refresh summary output folders.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.4.

Verification:

- `npm run data:summary -- --out .tmp/data-refresh --max-age-hours 48 --fail-on-stale`: passed; current generated cache is fresh, internally consistent, and has 170 metadata/README entries plus 60 release entries.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 25 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.

Notes:

- Generated cache files remain ignored. The workflows generate them in a clean checkout and expose freshness/integrity as artifacts instead of committing cache churn.
- Next roadmap item: `[ ] 7. Add stale asset and dead-code checks`.

## Batch 6: Stale Asset and Reference Audit

Completed roadmap item:

- `[x] 7. Add stale asset and dead-code checks`

Changes:

- Added `scripts/audit-assets.mjs`.
- Added `npm run assets:audit`.
- Made `npm run check` and `npm run build` run the asset/reference audit after data validation.
- Added deploy workflow coverage for the asset/reference audit.
- Added `archive/screenshots/README.md` with the screenshot retention and removal policy.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.5.

Verification:

- `npm run assets:audit`: passed; 22 expected live app screenshots, 22 tracked screenshots, 3 public scripts, 9 components, and 6 data modules checked.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run data:summary -- --out .tmp/data-refresh --max-age-hours 48 --fail-on-stale`: passed.
- `npm run check`: passed, 25 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.

Notes:

- Chose a lightweight custom audit over adding `knip` or ESLint in this batch because the repo is mostly Astro/static assets and the targeted risks are public screenshot drift plus orphaned source/public modules.
- Next roadmap item: `[ ] 8. Modernize CI quality gates`.

## Batch 7: Modern CI Quality Gates

Completed roadmap item:

- `[x] 8. Modernize CI quality gates`

Changes:

- Added `.github/dependabot.yml` for weekly npm and GitHub Actions update PRs.
- Added `.github/workflows/quality-gates.yml`.
- The quality workflow runs production audit reporting, catalog drift reporting, data validation, asset/reference audit, and Astro checks.
- The quality workflow uploads command logs as `quality-gate-reports`.
- The quality workflow opens or updates a GitHub issue if production audit or catalog drift fails.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.6.

Verification:

- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run check`: passed, 25 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.

Notes:

- The weekly workflow deliberately reports production audit/catalog drift without immediately hiding logs behind a failed step; it fails at the end after uploading artifacts and creating/updating an issue.
- Next roadmap item: `[ ] 9. Add proof-oriented project detail sections`.

## Batch 8: Proof-Oriented Project Details

Completed roadmap item:

- `[x] 9. Add proof-oriented project detail sections`

Changes:

- Added `src/data/proof.ts` with optional proof records for high-signal projects.
- Added `ProjectProof` and `ProjectProofSource` types.
- Added conditional Proof rendering to `src/pages/projects/[slug].astro`.
- Proof sections include problem, build evidence, platform support, install path, known limitations, and explicit source links.
- Extended `scripts/validate-project-data.mjs` to validate proof records and source URL shape.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.7.

Verification:

- `npm run data:validate`: passed; 6 proof records validated.
- `npm run assets:audit`: passed; 22 screenshots, 3 public scripts, 9 components, and 7 data modules checked.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 26 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 184 pages built.
- `git diff --check`: passed.
- Local preview screenshot: `http://127.0.0.1:4321/projects/sysadmindoc.github.io/#project-proof` captured to `.tmp/project-proof-section.png`; proof cards rendered without overlap at 1440px.

Notes:

- Started with six proof records rather than trying to annotate the full catalog; the model is now available for additional projects as source confidence permits.
- Next roadmap item: `[ ] 10. Build a year-in-review and project timeline layer`.

## Batch 9: Generated Timeline and Year-In-Review

Completed roadmap item:

- `[x] 10. Build a year-in-review and project timeline layer`

Changes:

- Added `src/pages/timeline.astro`.
- Generated yearly roll-up cards from cached GitHub releases, project push metadata, and tracked changelog highlights.
- Added a filterable timeline with year, platform, category, and language controls.
- Wired Timeline into homepage navigation, interior navigation, command palette quick links, and the Connect section.
- Fixed timeline build-time changelog loading to resolve from the repository root during Astro prerendering.
- Kept timeline filter state in-page instead of query-string URLs so static preview and GitHub Pages direct links stay reliable.
- Limited Astro build route concurrency to one to keep the large generated OG route set deterministic during local and CI builds.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.8.

Verification:

- `npm run data:validate`: passed; 6 proof records validated.
- `npm run assets:audit`: passed; 22 screenshots, 3 public scripts, 9 components, and 7 data modules checked.
- `npm run catalog:audit`: passed; no unreviewed active public repo drift found.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed, 27 files checked with 0 errors, 0 warnings, 0 hints.
- `npm run build`: passed, 185 pages built.
- `git diff --check`: passed.
- Browser QA via the in-app Browser plugin: `/timeline/#timeline-events` loaded with title `Timeline - Matt Parker`; console error/warn count was 0; platform `Android` plus year `2026` filtering showed 53 of 257 timeline events; URL remained `/timeline/#timeline-events`; screenshot confirmed the filter controls and first matching Android event rendered visibly.

Notes:

- This keeps the timeline static-first. No hosted search, analytics, or runtime API is required.
- The rendered QA pass caught and fixed two issues before commit: `CHANGELOG.md` resolution during Astro prerendering and reveal-animation hiding of the hash-linked filter section.
- Next roadmap item: `[ ] 11. Create a public-safe /til or notes feed only if there is durable source content`.

## Batch 10: Notes Feed Policy Decision

Completed roadmap item:

- `[x] 11. Create a public-safe /til or notes feed only if there is durable source content`

Changes:

- Added `NOTES_FEED_POLICY.md` as the durable decision record for the conditional notes/TIL feed.
- Documented why changelog history, generated GitHub metadata, `.ai/research/` artifacts, and local machine memory are not sufficient public note sources.
- Defined activation criteria for a future reviewed notes source, static notes route, validation, and RSS feed.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.9.

Verification:

- `npm run check`: passed; data validation, asset audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed, 185 pages built.
- `git diff --check`: passed.

Notes:

- No `/til` or `/notes` route was created because the roadmap explicitly required a durable source and review policy first, and this repository currently has policy material but no public-safe note corpus.
- Next roadmap item: `[ ] 12. Add an archive or anti-portfolio section for retired public projects`.

## Batch 11: Archive Decisions

Completed roadmap item:

- `[x] 12. Add an archive or anti-portfolio section for retired public projects`

Changes:

- Added `src/data/archive.ts` with typed archive-decision entries for moved, archived, removed, held, and superseded project surfaces.
- Added `/archive/`, a static public-safe anti-portfolio page.
- Wired Archive into homepage navigation, interior navigation, command palette defaults, quick links, and the Connect section.
- Extended `npm run data:validate` to validate archive entry IDs, statuses, source notes, safe root-relative or HTTPS links, and no-link sensitive holds.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.10.

Verification:

- `npm run check`: passed; data validation, asset audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed, 186 pages built.
- In-app Browser DOM QA: `/archive/` loaded with 6 entries, 2 no-link sensitive holds, 8 safe links, and the archive rules section.
- Local Playwright rendered QA: desktop and mobile `/archive/` passed with title `Archive - Matt Parker`, 6 entries, 2 no-link sensitive holds, 8 safe links, 5 status badges, and no console or page errors; screenshots captured to `.tmp/archive-desktop.png` and `.tmp/archive-mobile.png`.
- `git diff --check`: passed; Git reported only expected CRLF normalization warnings for touched markdown and source files.

Notes:

- Sensitive cleanup patterns are grouped without public links. The page links only to current public project routes or reviewed public GitHub repositories.
- Next roadmap item: `[ ] 13. Upgrade search beyond the current command palette dataset`.

## Batch 12: Static Full-Text Search

Completed roadmap item:

- `[x] 13. Upgrade search beyond the current command palette dataset`

Changes:

- Added Pagefind as a dev dependency.
- Updated `npm run build` to run `npm run search:index` after Astro emits `dist/`.
- Added `/search/`, a static Pagefind Component UI page with no-JS fallback links.
- Added `SEARCH_DECISION.md` comparing Pagefind, MiniSearch, Fuse.js, Lunr.js, and the existing command palette.
- Wired Search into homepage navigation, interior navigation, command palette defaults, quick links, and the Connect section.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.11.

Verification:

- `npm run check`: passed; data validation, asset audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed; Astro built 187 pages, then Pagefind v1.5.2 indexed 187 HTML files, 18,710 words, and wrote `dist/pagefind`.
- Generated search assets verified: `dist/search/index.html`, `dist/pagefind/pagefind-component-ui.js`, and `dist/pagefind/pagefind-component-ui.css`.
- In-app Browser QA: `/search/` loaded from the preview server with the expected heading, `pagefind-input`, and default summary. The in-app Browser blocked `/search/?q=Hyper-V` with `net::ERR_BLOCKED_BY_CLIENT`, so query-string preload remained unverified there.
- Local Playwright rendered QA: `/search/` passed desktop and mobile checks with no console or page errors; `SpotX`, `DICOM`, and `duplicate file` returned expected results. Screenshots captured to `.tmp/search-desktop.png` and `.tmp/search-mobile.png`.
- `git diff --check`: passed; Git reported only expected CRLF normalization warnings for touched markdown and source files.

Notes:

- The command palette remains the fast keyboard route for known project names and site sections. Pagefind handles full-text discovery over rendered pages and README excerpts.
- Next roadmap item: `[ ] 14. Audit Core Web Vitals, bfcache, and service-worker update UX`.

## Batch 13: Performance, bfcache, and Update UX

Completed roadmap item:

- `[x] 14. Audit Core Web Vitals, bfcache, and service-worker update UX`

Changes:

- Added `PERFORMANCE_AUDIT.md` with repeatable Chromium audit results, bfcache status, service-worker update behavior, and threshold notes.
- Added `npm run audit:perf` / `scripts/audit-performance.mjs` as a repeatable Chromium smoke audit for LCP, CLS, lab event timing, long tasks, overflow, console/network errors, and bfcache behavior.
- Updated `public/sw.js` to stop unconditionally calling `skipWaiting()` during install and to accept an explicit `SKIP_WAITING` message.
- Added a service-worker update toast in `public/scripts/main.js`; it appears when a waiting worker is installed and reloads only after the visitor chooses Refresh.
- Added update-toast styling in `src/styles/global.css`.
- Reserved homepage terminal body height and stable hero paragraph line-height/min-height to reduce first-viewport layout shift.
- Rewrote raw relative README image HTML to GitHub raw URLs and constrained project-detail mobile content after the audit caught a missing `logo.png` request and horizontal overflow on `/projects/project-nomad-desktop/`.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.12.

Verification:

- `npm run check`: passed; data validation, asset audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed from a cleaned generated `dist/`; Astro built 187 pages, then Pagefind v1.5.2 indexed 187 HTML files and 18,711 words.
- Earlier local Lighthouse samples completed and wrote JSON, but the Lighthouse CLI reported Windows temp cleanup `EPERM` after runs. The repeatable repo-local Chromium audit is the canonical item-14 gate.
- `npm run audit:perf`: completed against `http://127.0.0.1:4321`; samples for `/`, `/search/?q=NukeMap`, `/archive/`, `/projects/project-nomad-desktop/`, and desktop `/` all restored from bfcache, had no horizontal overflow, and had no console/network errors.
- Current metric highlights: mobile `/` LCP 3156ms / CLS 0 / max event 64ms; mobile `/search/?q=NukeMap` LCP 140ms / CLS 0 / max event 80ms; mobile `/archive/` LCP 76ms / CLS 0 / max event 32ms; mobile `/projects/project-nomad-desktop/` LCP 288ms / CLS 0 / max event 32ms; desktop `/` LCP 392ms / CLS 0.0398 / max event 64ms.

Notes:

- The audit harness reports threshold misses as warnings by default and can fail them with `--strict`; ignored JSON output remains under `.tmp/performance-audit.json`.
- Mobile homepage LCP remains the only current metric warning and should be handled as future first-viewport performance work rather than a blocker for the item-14 audit deliverable.
- Next roadmap item: `[ ] 15. Review image and OG generation pipeline`.

## Batch 14: Image and OG Pipeline

Completed roadmap item:

- `[x] 15. Review image and OG generation pipeline`

Changes:

- Added Sharp as a dev dependency for local image derivatives.
- Added `IMAGE_PIPELINE.md` as the operating record for social cards, live-app screenshots, thumbnail derivatives, README image handling, and the Astro image tooling decision.
- Added `npm run screenshots:thumbs` / `scripts/generate-screenshot-thumbnails.mjs` to regenerate 640x400 JPEG thumbnail derivatives from tracked live-app screenshot masters.
- Added `npm run images:audit` / `scripts/audit-image-pipeline.mjs` to validate screenshot master coverage, thumbnail coverage, image dimensions, size budgets, and OG PNG/social metadata constraints.
- Extended `npm run assets:audit` so missing or stale thumbnail derivatives fail alongside missing or stale screenshot masters.
- Updated `src/components/LiveCard.astro` and the client-side live-thumbnail fallback in `public/scripts/main.js` to prefer `public/screenshots/thumbs/*.jpg` card assets while preserving full screenshots for detail contexts.
- Added `og:image:type`, `og:image:alt`, and `twitter:image:alt` metadata in `src/layouts/Base.astro`.
- Kept the existing `src/pages/og/[slug].png.ts` Satori + Resvg endpoint at 1200x630 PNG instead of migrating to Astro image tooling.
- Fixed build-time public asset resolution in `LiveCard.astro` and project detail pages so static HTML emits tracked screenshots from the repo `public/` directory instead of falling back after Astro compilation.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.13.

Verification:

- `npm run screenshots:thumbs`: passed; generated 22 thumbnails, with thumbnails totaling 230.9 KB versus 1595.2 KB for screenshot masters.
- `npm run images:audit`: passed; 22 live apps checked, largest full screenshot 273.9 KB, largest thumbnail 37.3 KB, OG endpoint confirmed as 1200x630 PNG via Satori + Resvg, and social metadata confirmed as image/png with alt text.
- `npm run assets:audit`: passed; 22 screenshot masters and 22 thumbnails checked.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run check`: passed; data validation, asset audit, image audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run build`: passed from a cleaned generated `dist/`; Astro built 187 pages, then Pagefind v1.5.2 indexed 187 HTML files and 18,712 words.
- Local Playwright rendered smoke: homepage live cards use `/screenshots/thumbs/*.jpg`, `/projects/StormviewRadar/` uses `/screenshots/StormviewRadar.jpg`, and `/og/StormviewRadar.png` returns a PNG with matching social metadata.

Notes:

- Thumbnail derivatives are tracked under `public/screenshots/thumbs/` so GitHub Pages can serve compact card previews without runtime image processing.
- Next roadmap item: `[ ] 16. Add public portfolio feeds and machine-readable index files`.

## Batch 15: Public JSON Indexes

Completed roadmap item:

- `[x] 16. Add public portfolio feeds and machine-readable index files`

Changes:

- Added `src/pages/projects.json.ts`, a schema-versioned static project index generated from `src/data/projects.ts`, live-app data, category labels, and ignored GitHub metadata caches when present.
- Added `src/pages/releases.json.ts`, a schema-versioned static release index generated from the cached GitHub release data.
- Included freshness timestamps, counts, project detail URLs, repository URLs, live URLs, OG image URLs, screenshot/thumbnail URLs, star counts, and pushed/updated metadata where available.
- Advertised both JSON indexes through shared `<link rel="alternate" type="application/json">` tags in `src/layouts/Base.astro`.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.14.

Verification:

- `npm run check`: passed; data validation, asset audit, image audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed; Astro emitted `/projects.json` and `/releases.json`, then Pagefind indexed 187 HTML files and 18,712 words.
- Generated JSON smoke check: `dist/projects.json` schemaVersion 1 with 173 projects, 22 live entries, count consistency, and a thumbnail URL for `StormviewRadar`.
- Generated JSON smoke check: `dist/releases.json` schemaVersion 1 with 60 releases across 21 repositories and count consistency.
- Rendered head check: `dist/index.html` includes alternate JSON links for `/projects.json` and `/releases.json`.

Notes:

- The endpoints stay static and public-only. They expose only data already represented by the public portfolio and GitHub metadata cache.
- Next roadmap item: `[ ] 17. Evaluate local semantic indexing for project organization`.

## Batch 16: Semantic Index Evaluation

Completed roadmap item:

- `[x] 17. Evaluate local semantic indexing for project organization`

Changes:

- Added `SEMANTIC_INDEX_DECISION.md` as the operating decision record for semantic indexing, privacy boundaries, and why hosted semantic search or client-side embeddings are not being added right now.
- Added `npm run semantic:audit` / `scripts/audit-semantic-index.mjs`, a deterministic local token-similarity report over public project metadata plus ignored cached README text when present.
- The audit reports top similar-project pairs and cross-category review candidates for catalog maintenance, duplicate positioning review, and future related-project planning.
- Kept Pagefind as the user-facing static search layer and did not add hosted inference, visitor tracking, client-side embeddings, or committed model artifacts.
- Updated README, changelog, project context, roadmap state, and package version to v0.16.15.

Verification:

- `npm run semantic:audit -- --limit 12`: passed; checked 173 projects, found 165 usable cached README texts, and emitted advisory similar-project/category-drift candidates.
- `npm run check`: passed; data validation, asset audit, image audit, and Astro diagnostics completed with 0 errors, 0 warnings, and 0 hints.
- `npm run audit:prod`: passed, 0 production vulnerabilities.
- `npm run build`: passed; Astro built 187 pages and Pagefind indexed 187 HTML files and 18,713 words.
- `git diff --check`: passed with CRLF conversion warnings only.

Notes:

- All active roadmap checklist items are now complete. Future work should come from new evidence, dependency/security drift, or an explicit decision to revive a parked item.
