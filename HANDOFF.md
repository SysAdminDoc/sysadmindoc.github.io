# Handoff — sysadmindoc.github.io

Copy-paste the block below into a new Claude Code chat to continue building the portfolio.

---

## Prompt for new chat

I'm continuing work on my portfolio at `~/repos/sysadmindoc.github.io/`, deployed to https://sysadmindoc.github.io/.

**Current state: v0.7.0, shipped and live.** Full state-of-the-project:

- **Stack**: Astro 5 + TypeScript + vanilla JS. 150 static pages built in ~28s. Deploys via GitHub Actions on push to `main` + daily cron at 07:00 UTC that refreshes GitHub API data.
- **Structure**: `src/data/projects.ts` (auto-extracted) + `src/data/curated.ts` (hand-curated content). Components in `src/components/`. Routes: `/`, `/now`, `/healthcare-it`, `/releases`, `/lang/<slug>` (7 langs), `/projects/<slug>` (138 project pages), `/og/<slug>.png` (139 satori-generated OG cards), `/rss.xml`, `/sitemap-index.xml`.
- **Content layer built at CI**:
  - `_stars.json` — star counts per repo
  - `_stats.json` — totals, streak, last push, latest release, fetched-at timestamp
  - `_meta.json` — pushed_at, updated_at, language per repo
  - `_releases.json` — 71 releases from top 40 most-recently-pushed repos
  - All gitignored, regenerated by `npm run fetch-stars` in CI
- **Assets**: 1.5 MB of screenshots (`public/screenshots/<slug>.jpg` × 23 live apps via Playwright), 15 MB of OG cards (built at Astro build time via satori).

**Read before touching anything:**
1. `ROADMAP.md` — ranked plan, what's next
2. `ROADMAP-COMPLETED.md` — shipped releases
3. `CHANGELOG.md` — detailed notes per version
4. `README.md` — structure summary
5. `~/CLAUDE.md` — global rules (PowerShell/Python/etc conventions, git, versioning)

**Priorities for this session — focus on `/projects/<slug>` page quality first.**

The 138 project detail pages currently show: name, category, tags, GitHub link, live-demo link (if applicable), star count, and either a captured screenshot (for live apps) or the satori OG card. They're technically functional but thin. Improvements in priority order:

1. **Render actual README content**, not just a link out. Fetch each repo's README.md at build time in `scripts/fetch-stars.mjs` (or a new `scripts/fetch-readmes.mjs`), cache to `src/data/_readmes.json`, render via `marked` or `@astrojs/markdown-remark`. Sanitize HTML. Graceful fallback when a repo has no README.
2. **Per-project releases** — extract the subset of `_releases.json` that matches this slug, show last 5 inline on the project page with tag, date, and notes excerpt.
3. **Per-project OG card tuning** — some entries have long names that overflow satori's 1080px cap. Add line-clamping, auto-shrink font for long titles. File: `src/pages/og/[slug].png.ts`.
4. **Tech stack detection** — infer from the repo's `language` field plus catalog `category`. Display as chips on project pages (e.g., Kotlin + Android + Material 3).
5. **"Related projects" improvement** — currently just first 6 of same category. Rank by stars/freshness; exclude archived; show at most 4. Maybe a "More like this" carousel.
6. **Install/usage snippets** from the README's first code block. Quick start without leaving the page.

**Other queued work from ROADMAP.md v0.8.0 (creative depth)** — only touch after the project-page improvements above:
- Year-in-Review post template
- Series arcs with completion bars (e.g., "Windows debloat suite: 7 of 9 shipped")
- Anti-portfolio/Graveyard for archived repos
- Yearly archive snapshots (XL — build-pipeline work)
- Tools gallery with WASM demos (XL per tool)

**Parallel tech debt** (roadmap's "Technical debt & polish" section) — tackle opportunistically:
- OG card size (15 MB → aim for ~7 MB, try WebP)
- Complete the light-theme palette (currently only color vars override; nav/hero/sections still dark-tuned). Or remove the toggle — my pick is **finish it**, not remove.
- Sitemap verification — make sure `/projects/<slug>/` and `/lang/<slug>/` are all indexed.
- `src/pages/projects/[slug].astro` still imports `node:fs`/`node:path`/`node:url` at the top which works for static build but feels brittle. Could use Astro content collections with Zod schema instead.
- Fuzzy search upgrade in cmdk.js (current subsequence matcher is fine but Fuse.js handles typos better).

**Working conventions that matter here:**
- User grants full autonomy to install/uninstall/alter PC without asking
- `~/CLAUDE.md` has mandatory per-language rules — read it
- Every change gets a version bump (semver), CHANGELOG entry, git tag, GitHub release with notes. Same-commit doc sync.
- No Co-Authored-By in commit messages. Ever.
- Commits focused on *why*, not *what*
- Dense terse output. No scope creep. If tests or types fail, fix at the root cause, don't bypass.
- Branch protection is on for `main` — go ahead and push directly; don't force-push
- `npm run capture-screenshots` is manual (Playwright + local Chromium); don't move it to CI without permission (170MB container)

**Verify before starting:**

```bash
cd ~/repos/sysadmindoc.github.io
git status                     # should be clean on main
git log --oneline -3           # should show v0.7.0 latest
npm run build                  # should finish in ~28s with 150 pages
```

Then pick item #1 from the priority list above (render README content on project pages) and execute end-to-end: implement, build, verify, commit, tag, release, push — same pattern as every prior version.

---

## Quick reference

- Live site: https://sysadmindoc.github.io/
- Repo: https://github.com/SysAdminDoc/sysadmindoc.github.io
- CI workflow: `.github/workflows/deploy.yml`
- Releases page (meta): https://github.com/SysAdminDoc/sysadmindoc.github.io/releases
- Stats refresh: daily 07:00 UTC or manual `Actions → Deploy portfolio → Run workflow`
